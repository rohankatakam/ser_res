# Serafis — Centralized environment (single source for backend, evaluation, Docker)
# Copy this file to .env in this directory and fill in your values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# API Keys
# -----------------------------------------------------------------------------

# OpenAI — embeddings (backend) and LLM judge (evaluation)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Gemini — LLM-as-judge (evaluation, optional)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic — LLM-as-judge (evaluation, optional)
# https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# Server (backend)
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000

# Vector store: Pinecone (required). Embeddings are fetched by episode id; no full load into memory.
# https://app.pinecone.io/organizations/-/keys
PINECONE_API_KEY=your-pinecone-api-key-here
# rec_for_you index (separate from metaspark RAG indexes). Default: rec-for-you
# PINECONE_REC_FOR_YOU_INDEX=rec-for-you

# Paths — override only if not using repo layout (docker-compose sets these in container)
# ALGORITHMS_DIR=./algorithm
# FIXTURES_DIR=./evaluation/fixtures
# CACHE_DIR=./cache
# EVALUATION_DIR=./evaluation

# -----------------------------------------------------------------------------
# Firestore (episodes, series, users, engagements)
# -----------------------------------------------------------------------------
# Path to Firebase service account JSON (Firebase Console > Service accounts > Generate new key).
# Same path for local and Docker; compose mounts this file into the container. Do not commit.
# Alternative: GOOGLE_APPLICATION_CREDENTIALS (standard env for Google SDKs) is used as fallback.
FIREBASE_CREDENTIALS_PATH=./secrets/firebase-credentials.json
# Optional: Firebase project ID (else read from the credentials JSON)
# FIREBASE_PROJECT_ID=your-project-id
# Firestore collections (metaspark-aligned). Defaults: podcast_episodes, podcast_series
# FIRESTORE_EPISODES_COLLECTION=podcast_episodes
# FIRESTORE_SERIES_COLLECTION=podcast_series

# -----------------------------------------------------------------------------
# Evaluation (runner, LLM judges)
# -----------------------------------------------------------------------------
# API Server URL when running evaluation from host (default: backend on localhost)
# API_URL=http://localhost:8000
