# Serafis — Centralized environment (single source for backend, evaluation, Docker)
# Copy this file to .env in this directory and fill in your values:
#   cp .env.example .env

# -----------------------------------------------------------------------------
# API Keys
# -----------------------------------------------------------------------------

# OpenAI — embeddings (backend) and LLM judge (evaluation)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Gemini — LLM-as-judge (evaluation, optional)
# https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic — LLM-as-judge (evaluation, optional)
# https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# Server (backend)
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000

# Qdrant (Docker Compose sets QDRANT_URL for the backend container)
# QDRANT_URL=http://localhost:6333

# Paths — override only if not using repo layout (docker-compose sets these in container)
# ALGORITHMS_DIR=./algorithm
# DATASETS_DIR=./datasets
# CACHE_DIR=./cache
# EVALUATION_DIR=./evaluation

# -----------------------------------------------------------------------------
# Data source: episodes, series, and user persistence
# -----------------------------------------------------------------------------
# One of: json | firebase  (leave unset to use dataset from config/load for episodes)
# DATA_SOURCE=json
# DATA_SOURCE=firebase

# When DATA_SOURCE=json — paths to episodes, series, and users JSON files (required if DATA_SOURCE=json)
# EPISODES_JSON_PATH=./datasets/eval_909_feb2026/episodes.json
# SERIES_JSON_PATH=./datasets/eval_909_feb2026/series.json
# USERS_JSON_PATH=./data/users.json

# When DATA_SOURCE=firebase — Firebase/Firestore (required if DATA_SOURCE=firebase)
# Path to service account JSON key (Firebase Console > Project Settings > Service Accounts > Generate new key).
# Local runs: set FIREBASE_CREDENTIALS_PATH to your key file path.
# Docker: set FIREBASE_CREDENTIALS_FILE to the *host* path (used to mount the file); default is ./secrets/firebase-credentials.json — put your key there and never commit it.
# FIREBASE_CREDENTIALS_PATH=./secrets/firebase-credentials.json
# FIREBASE_CREDENTIALS_FILE=./secrets/firebase-credentials.json
# Or use GOOGLE_APPLICATION_CREDENTIALS (same file path).
# Optional: Firebase project ID (else read from the credentials JSON)
# FIREBASE_PROJECT_ID=your-project-id

# -----------------------------------------------------------------------------
# Evaluation (runner, LLM judges)
# -----------------------------------------------------------------------------
# API Server URL when running evaluation from host (default: backend on localhost)
# API_URL=http://localhost:8000
